version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app
      - ./data:/usr/src/app/data
    environment:
      - NODE_ENV=development
      - PORT=3000
    command: npm run dev
